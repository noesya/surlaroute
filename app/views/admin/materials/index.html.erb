<%= content_for :title, "#{Material.model_name.human(count: 2)} (#{@materials.total_count})" %>

<%= render 'sort_by_form' %>

<%= render 'filters', current_path: admin_materials_path %>

<%= render 'admin/materials/list', materials: @materials %>
<%= paginate @materials, theme: 'bootstrap-5' %>

<% content_for :buttons do %>
  <%= create_link Material %>
<% end %>