<%= content_for :title, "#{Technic.model_name.human(count: 2)} (#{@technics.total_count})" %>

<%= render 'filters', current_path: admin_technics_path %>

<%= render 'admin/technics/list', technics: @technics %>
<%= paginate @technics, theme: 'bootstrap-5' %>

<% content_for :buttons do %>
  <%= create_link Technic %>
<% end %>