<%-
searchable_text_parts = []
elements = data.dig('elements') || []
elements.each do |element|
  searchable_text_parts << element.dig('caption')
end
-%>
<%= searchable_text_parts.compact.join(' ') %>