<%= content_for :title, @criterion %>

<%= show @criterion, :kind %>
<%= show @criterion, :about_class %>
<%= show @criterion, :hint %>

<% content_for :buttons do %>
  <%= destroy_link @criterion %>
  <%= edit_link @criterion %>
<% end %>
