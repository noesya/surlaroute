<%= f.input filter[:scope_name], 
            as: :string, 
            label: filter[:label],
            required: false,
            input_html: { 
              html5: true,
              value: filter[:value_method].call(filter, params),
              class: "mb-#{filter[:margin_bottom]} filter flatpickr",
              autocomplete: 'one-time-code'
            },
            wrapper_html: { class: filter[:class] }
%>