<% files = item.files_for(about) %>

<% if files.count > 2 %>
  <div class="splide" aria-label="Galerie">
    <div class="splide__track">
      <ul class="splide__list">
        <% files.each do |file| %>
          <% next unless file.file.attached? %>
          <li class="splide__slide col-md-3 col-lg-6">
            <figure>
              <%= kamifusen_tag file.file,
                                class: 'img-fluid rounded',
                                alt: file.alt %>
              <% if file.text? %>
                <figcaption class="mt-1 small"><%= file.text %></figcaption>
              <% end %>
            </figure>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
<% else %>
  <div class="row">
    <% files.each do |file| %>
      <% next unless file.file.attached? %>
      <div class="col-md-3 col-lg-6">
        <figure>
          <%= kamifusen_tag file.file,
                            class: 'img-fluid rounded',
                            alt: file.alt %>
          <% if file.text? %>
            <figcaption class="mt-1 small"><%= file.text %></figcaption>
          <% end %>
        </figure>
      </div>
    <% end %>
  </div>
<% end %>
