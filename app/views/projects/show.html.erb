<div class="container">
  <div class="row">
    <div class="col-lg-6">
      <%= kamifusen_tag @project.image, 
                        class: 'img-fluid rounded square', 
                        alt: @project if @project.image.attached? %>
    </div>
    <div class="col-lg-6">
      <%= render 'structure/title_with_structure', about: @project %>
      <p class="mb-5"><%= sanitize @project.description %></p>
      <%= render 'application/meta', about: @project %>
      <hr class="my-5">
      <%= render 'structure/items/zones/header', about: @project %>
      <%= render 'application/regions', about: @project %>
    </div>
  </div>
  <%= render 'structure/items/zones/page', about: @project %>
</div>

<% if @project.materials %>
  <section class="bg-white py-5">
    <div class="container">
      <h2><%= t('ui.project.materials', count: @project.materials.count) %></h2>
      <%= render 'materials/list', objects: @project.materials %>
    </div>
  </section>
<% end %>

<% if @project.actors %>
  <section class="bg-white py-5">
    <div class="container">
      <h2><%= t('ui.project.actors', count: @project.actors.count) %></h2>
      <%= render 'actors/list', objects: @project.actors %>
    </div>
  </section>
<% end %>